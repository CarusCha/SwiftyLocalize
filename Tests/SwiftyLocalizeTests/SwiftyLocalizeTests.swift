    import XCTest
    @testable import SwiftyLocalize

    final class SwiftyLocalizeTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftyLocalize().text, "Hello, World!")
        }
    }
